# Host Configuration

Host-level device rules and permissions that must live on the host rather
than inside a container (see main README §8, §16).

- `udev/` — udev rules for serial/USB/camera device permissions.

Nothing here yet — no device-specific rules have been needed so far.
